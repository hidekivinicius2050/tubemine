SELECT id, name, email FROM users WHERE email = 'hideki@gmail.com';

SELECT * FROM subscriptions WHERE user_id = 1 ORDER BY created_at DESC;

SELECT s1.user_id, s1.plan_type, s1.status FROM subscriptions s1 
LEFT JOIN subscriptions s2 ON s1.user_id = s2.user_id AND s1.created_at < s2.created_at 
WHERE s2.user_id IS NULL AND s1.user_id = 1;
